{
  "name": "safeweb-proxy",
  "version": "1.0.0",
  "description": "Proxy server for Google Places API to avoid CORS issues",
  "main": "proxy-server.js",
  "scripts": {
    "start": "node proxy-server.js"
  },
  "dependencies": {
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "node-fetch": "^2.6.7"
  }
}