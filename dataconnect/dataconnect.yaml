specVersion: "v1"
serviceId: "saferide-app"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "saferideapplication-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
